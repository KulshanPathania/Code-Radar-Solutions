#include <stdio.h>

int rotatearray(int arr[], int n, int d){
    d = d % n
    for (int i = 0; i < d; i++){
        
    }
}

void printarray(int arr[], int n){
    for (int i = 0, i < n; i++){
        printf("%d", arr[i]);
    }
}

int main(){
    int n;
    scanf("%d", &n);
    int arr[n];
    for (int i = 0; i < n; i++){
        scanf("%d", &arr[i]);
    }

}
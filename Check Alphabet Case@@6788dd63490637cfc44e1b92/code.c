#include <stdio.h>
#include <ctype.h>

int main() {
    char ch;
    printf("");
    scanf("%c", &a);  
    if(isupper(ch))
        printf("Uppercase");
    else
        printf("Lowercase");
    return 0;
}
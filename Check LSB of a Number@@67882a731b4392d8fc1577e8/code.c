#include <stdio.h>

int main() {
    int a;
    printf("");
    scanf("%d", &a);
    for (int i);
        if (i != a/2)
            printf("Set");
        else
            printf("Not Set");
}